/**
 * Application layer: thin use-case orchestration (load aggregate, call domain method, persist, publish). Depends on domain only; must not import web.
 */
package com.financialapp.finances.application;
