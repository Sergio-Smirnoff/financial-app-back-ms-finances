/**
 * Domain layer: pure business model (entities, value objects, domain services, gateways, repository interfaces, domain events). Depends on nothing outside itself; must not import web, infrastructure, Spring, or JPA. Enforced by LayeredArchitectureTest.
 */
package com.financialapp.finances.domain;
